# No env vars required for the static client alone.
# When integrating waitlist with the main PayPerCall backend, use Vite env vars
# (e.g. VITE_API_BASE_URL) and update WaitlistModal fetch URL as needed.
